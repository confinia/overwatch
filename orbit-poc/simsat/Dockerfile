# simsat — the simulated satellite (#260). Stdlib only: the image is python,
# the script, nothing else.
FROM docker.io/library/python:3.12-slim
WORKDIR /app
COPY simsat.py .
CMD ["python3", "simsat.py"]
